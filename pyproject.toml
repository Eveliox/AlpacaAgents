[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "alpaca-agents"
version = "0.1.0"
description = "Paper-only options swing trading safety foundation"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
alpaca_agents = ["assets/*.png", "assets/*.js"]
